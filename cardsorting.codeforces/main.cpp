#include <bits/stdc++.h>
using namespace std;



int main() {

    int n;
    vector<int> v;
    cin >> n;
    int val;

    while(n--){
        cin >> val;
        v.push_back(val);
    }

    cout << i << endl;

    return 0;
}
